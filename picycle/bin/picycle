#!/usr/bin/env ruby

lib = File.expand_path('../../lib', __FILE__)
$LOAD_PATH.unshift(lib) unless $LOAD_PATH.include?(lib)

require 'picycle'

devmode = ARGV[0] && ARGV[0] == 'devmode' ? true : false
Picycle.new(devmode).run
